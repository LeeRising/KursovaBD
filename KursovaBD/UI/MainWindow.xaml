<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursovaBD"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:Custom="http://metro.mahapps.com/winfx/xaml/controls" x:Class="KursovaBD.MainWindow"
        mc:Ignorable="d"
        ResizeMode="CanMinimize"
        WindowStartupLocation="CenterScreen"
        Title="Dogs Show" Height="550" Width="725"
        Background="{DynamicResource MaterialDesignPaper}"
        >
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="5" />
            <ColumnDefinition Width="520" />
        </Grid.ColumnDefinitions>
        <StackPanel Background="Gray" x:Name="MainMenu" Width="200" 
                    Grid.Column="0">
            <Button Content="Test" x:Name="btn"/>
        </StackPanel>
        <GridSplitter ResizeDirection="Columns" ResizeBehavior="PreviousAndNext"
                      Grid.Column="1" HorizontalAlignment="Stretch"/>
        <Grid Background="Aqua" x:Name="MainWindowContent" Width="520" 
              Grid.Column="2">
        </Grid>
    </Grid>
</Window>
