<Window x:Class="KursovaBD.Views.Dialogs.ExpertChooserDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KursovaBD.Views.Dialogs"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        ResizeMode="CanMinimize"
        WindowStyle="None"
        WindowStartupLocation="CenterScreen"
        Height="170" Width="270">
    <Grid>
        <ComboBox x:Name="Experts" materialDesign:HintAssist.Hint="Choose expert" HorizontalAlignment="Left" Margin="10,50,0,0" VerticalAlignment="Top" Width="190"/>
        <Button x:Name="AddBtn" HorizontalAlignment="Left" Margin="212,50,0,0" VerticalAlignment="Top" Height="28">
            <materialDesign:PackIcon Kind="PlusCircleOutline"/>
        </Button>
        <StackPanel x:Name="ShowAddExpertPanel" ScrollViewer.VerticalScrollBarVisibility="Visible" Background="WhiteSmoke" Margin="0,90,0,0"/>
        <Button x:Name="BackBtn" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="5,5">
            <materialDesign:PackIcon Kind="ArrowLeft"/>
        </Button>
    </Grid>
</Window>
